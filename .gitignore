# Dependencies
node_modules
.pnpm-store

# Local Environment & Secrets (DO NOT COMMIT)
.env.local
.env.*.local
# Timestamped backups written by scripts/sync-env-to-vercel.ts (belt-and-braces
# alongside `.env*` below, so reverting that line cannot expose a secret file)
.env*.backup.*

# Vercel & Build Artifacts
.vercel
dist

# Raw Data Imports (Do not commit price lists)
*.csv
.env*
